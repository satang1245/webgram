# 함수(Function)
---

`(변수:티입, 변수: 타입): 리턴타입` // 리턴타입은 생략가능.

```ts
function sum(a: number, b: number): number {
	return a + b;
}
const sum = function() (a: number, b: number): number { return a + b }

const sum = (a: number, b: number): number => a+b;

```
